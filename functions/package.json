{
  "name": "certifyos-revert-logger",
  "version": "1.0.0",
  "description": "Cloud Function to log CertifyOS workflow revert API usage to BigQuery",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test test/*.test.mjs",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "deploy": "npm run build && gcloud functions deploy certifyos-revert-logger --runtime nodejs20 --trigger-http --allow-unauthenticated --entry-point logUsage --source . --region us-central1"
  },
  "dependencies": {
    "@google-cloud/bigquery": "^7.3.0",
    "@google-cloud/functions-framework": "^3.3.0",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": "20"
  }
}
